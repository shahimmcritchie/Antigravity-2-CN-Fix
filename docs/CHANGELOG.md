# 更新日志

## [0.1.0] - 2026-05-20

### 新增
- 项目初始化
- NLS 文本提取 (16,567 条消息)
- 项目目录结构搭建
- 自动化脚本框架
- 文档体系建立

## [0.2.0] - 2026-05-20

### 修复
- 修复 AI UI 注入方案导致的“进入界面卡死”问题。
- 将高风险的全局 `https` 拦截改为仅重定向 `/main.js` 的 `agy-ui://` 方案。
- 在本机自动升级到 `2.0.1` 后，重新把 AI UI 注入挂回当前 `app.asar`，恢复中文主界面加载。

### 改进
- `scripts/translate_ui.py` 扩展到 289 条 AI UI 文案映射，本机最新一轮命中 251 条。
- 设置中心补充首轮汉化，覆盖 `外观 / 权限 / 浏览器 / 模型 / 自定义 / 应用 / 账户 / 快捷键` 等主要导航与设置项。
- 项目设置页继续补齐，已覆盖 `文件夹 / 添加文件夹 / 智能体设置 / 安全预设 / 产物评审策略 / 本地权限 / 文件访问规则 / 网络访问规则 / 沙箱外命令 / MCP 工具 / 危险区域 / 删除项目` 等高频英文。
- 账号页、应用页、快捷键页的主要固定说明文案已补齐。
- README、安装说明、任务记录补充当前状态、已知问题与版本变化说明。

### 已知问题
- `scripts/apply.ps1` 仍按 `2.0.0` 目录结构判断安装存在性，遇到 `2.0.1` 会误报未安装。
- `2.0.1` 的核心 NLS / product / extension 自动应用流程还没有完成适配。

## [0.3.0] - 2026-05-23

### 修复
- 将 `scripts/apply.ps1` 更新为兼容 `Antigravity 2.0.6` 的部署逻辑。
- `-EnableAiUi` 现在会优先从运行中的 `2.0.6` 客户端抓取真实 `/main.js`，再做哈希校验与 AI UI 注入。

### 改进
- `patches/ai-ui-compat.json` 已补充 `2.0.6` 兼容签名。
- 新增 [implementation_plan.md](implementation_plan.md)，记录 `2.0.6` 的实施状态与标准部署方式。
- README、安装说明、开发交接文档已统一切换到 `2.0.6` 口径。
